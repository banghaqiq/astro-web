---
const { title, content } = Astro.props;
---
<div class="container-fluid">
  <div class="container">
    <h1 class="title">{title}</h1>
    <p class="content">{content}</p>
  </div>
</div>

<style>
  .container-fluid {
    text-align:center;
    display: flex;
    justify-content: center; /* Menengah horizontal */
    align-items: center;     /* Menengah vertikal */
    height: 100vh;           /* Memastikan container memiliki tinggi viewport */
  }
  .title{
    cursor:pointer;
    font-weight:bolder;
    font-family:rubik;
    text-decoration:underline;
  }
  .content{
    font-weight:bolder;
    font-family:rubik;
  }
</style>